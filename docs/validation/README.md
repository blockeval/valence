# Validation notes

The authoritative public baseline is [`VALIDATION_v0.8.md`](../../VALIDATION_v0.8.md).
The v0.7 report remains available as the explicit availability/fork/finality baseline.
Its deterministic reference artifacts are under `validation/reference/v0.7/`.

The frozen v0.6 paper experiments remain under `validation/reference/v0.6/` and
are retained unchanged for reproducibility. Earlier engineering notes are
historical and should not supersede the current validation report.
